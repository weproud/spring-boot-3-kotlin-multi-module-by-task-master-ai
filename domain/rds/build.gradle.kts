dependencies {
    // Core and domain common dependencies
    implementation(project(":core"))
    implementation(project(":domain:common"))
    
    // Spring Data JPA is applied from the root build.gradle.kts
}
